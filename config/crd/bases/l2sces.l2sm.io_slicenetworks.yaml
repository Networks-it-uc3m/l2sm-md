# Copyright 2024 Universidad Carlos III de Madrid
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: slicenetworks.l2sces.l2sm.io
spec:
  group: l2sces.l2sm.io
  names:
    kind: SliceNetwork
    listKind: SliceNetworkList
    plural: slicenetworks
    singular: slicenetwork
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SliceNetwork is the Schema for the slicenetworks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SliceNetwork
            properties:
              clusters:
                description: |-
                  Clusters is an array of configurations. The controller will create an L2Network
                  in each of these clusters based on the provided Name, Type, and Provider.
                items:
                  type: string
                minItems: 1
                type: array
              provider:
                description: Provider defines the provider's name and domain for the
                  network in this cluster.
                properties:
                  dnsGrpcPort:
                    default: "30818"
                    description: gRPC management port for DNS service (used for adding/modifying
                      DNS entries)
                    type: string
                  dnsPort:
                    default: "30053"
                    description: |-
                      DNS service configuration
                      DNS protocol port (used for DNS queries via tools like dig)
                    type: string
                  domain:
                    items:
                      type: string
                    type: array
                  name:
                    type: string
                  ofPort:
                    default: "6633"
                    type: string
                  sdnPort:
                    default: "30808"
                    type: string
                required:
                - domain
                - name
                type: object
              type:
                description: Type specifies the type of L2Network to create in this
                  cluster (e.g., ext-vnet, vnet, vlink).
                enum:
                - ext-vnet
                - vnet
                - vlink
                type: string
                x-kubernetes-preserve-unknown-fields: true
            required:
            - clusters
            - type
            type: object
          status:
            description: status defines the observed state of SliceNetwork
            properties:
              clusterStatuses:
                description: ClusterStatuses tracks the status of the L2Network provisioning
                  in each defined cluster.
                items:
                  description: SliceClusterStatus tracks the state of the L2Network
                    in a specific cluster.
                  properties:
                    clusterName:
                      description: ClusterName is the name of the cluster this status
                        refers to.
                      type: string
                    message:
                      description: Message provides details about the status, useful
                        for debugging errors.
                      type: string
                    status:
                      description: Status represents the current state of the L2Network
                        in that cluster (e.g., "Ready", "Pending", "Error").
                      type: string
                  required:
                  - clusterName
                  type: object
                type: array
              conditions:
                description: Conditions represent the current global state of the
                  SliceNetwork resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
